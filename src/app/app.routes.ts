import { Routes } from '@angular/router';

// 🗺️ Configuración de Rutas
// En este archivo definirás las rutas de tu sitio para navegar entre componentes.
// Ejemplo para seguir creando: { path: 'inicio', loadComponent: () => import('./inicio/inicio.component').then(c => c.InicioComponent) }
// Agrega tus rutas dentro del siguiente array vacío:
export const routes: Routes = [];
